CREATE TRIGGER T_DAP_BI_0
  BEFORE INSERT
  ON `dap`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('dap', 'progressivo');
END IF;

IF NEW.RATA = 0 THEN
  CALL P_CAMPO_VUOTO('dap', 'rata');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
