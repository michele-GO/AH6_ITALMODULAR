CREATE TRIGGER T_BVP_BI_0
  BEFORE INSERT
  ON `bvp`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('bvp', 'progressivo');
END IF;

IF NEW.RATA = 0 THEN
  CALL P_CAMPO_VUOTO('bvp', 'rata');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
