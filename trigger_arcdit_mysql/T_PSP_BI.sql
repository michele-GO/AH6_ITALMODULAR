CREATE TRIGGER T_PSP_BI_0
  BEFORE INSERT
  ON `psp`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
 CALL P_CAMPO_VUOTO('psp','progressivo');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
