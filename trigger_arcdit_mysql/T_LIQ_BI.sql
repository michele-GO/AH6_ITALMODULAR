CREATE TRIGGER T_LIQ_BI_0
  BEFORE INSERT
  ON `liq`
  FOR EACH ROW
BEGIN 


IF NEW.ANNO = 0 THEN
  CALL P_CAMPO_VUOTO('liq','anno');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
