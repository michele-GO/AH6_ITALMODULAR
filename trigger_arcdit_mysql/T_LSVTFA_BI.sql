CREATE TRIGGER T_LSVTFA_BI_0
  BEFORE INSERT
  ON `lsvtfa`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('lsvtfa', 'progressivo');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
