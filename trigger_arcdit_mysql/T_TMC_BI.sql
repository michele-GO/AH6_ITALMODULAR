CREATE TRIGGER T_TMC_BI_0
  BEFORE INSERT
  ON `tmc`
  FOR EACH ROW
BEGIN 


IF NEW.CODICE = '' THEN
  CALL P_CAMPO_VUOTO('tmc','codice');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
