CREATE TRIGGER T_BVCMRT_BI_0
  BEFORE INSERT
  ON `bvcmrt`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('bvcmrt', 'progressivo');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
