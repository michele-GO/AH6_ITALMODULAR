CREATE TRIGGER T_TNT_BI_0
  BEFORE INSERT
  ON `tnt`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0  THEN
  CALL P_CAMPO_VUOTO('tnt', 'progressivo');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
