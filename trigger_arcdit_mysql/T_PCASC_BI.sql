CREATE TRIGGER T_PCASC_BI_0
  BEFORE INSERT
  ON `pcasc`
  FOR EACH ROW
BEGIN 


IF NEW.ID_PCA = 0 THEN
  CALL P_CAMPO_VUOTO('pcasc', 'id_pca');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
