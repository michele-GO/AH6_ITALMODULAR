CREATE TRIGGER T_TVA_BI_0
  BEFORE INSERT
  ON `tva`
  FOR EACH ROW
BEGIN 


IF NEW.CODICE = '' THEN
  CALL P_CAMPO_VUOTO('tva','codice');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
