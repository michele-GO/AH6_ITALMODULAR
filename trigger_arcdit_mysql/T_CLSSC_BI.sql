CREATE TRIGGER T_CLSSC_BI_0
  BEFORE INSERT
  ON `clssc`
  FOR EACH ROW
BEGIN 


IF NEW.ID_CLS = 0 THEN
  CALL P_CAMPO_VUOTO('clssc', 'id_cls');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
