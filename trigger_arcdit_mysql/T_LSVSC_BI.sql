CREATE TRIGGER T_LSVSC_BI_0
  BEFORE INSERT
  ON `lsvsc`
  FOR EACH ROW
BEGIN 


IF NEW.ID_LSV = 0 THEN
  CALL P_CAMPO_VUOTO('lsvsc', 'id_lsv');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
