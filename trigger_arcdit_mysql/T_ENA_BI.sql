CREATE TRIGGER T_ENA_BI_0
  BEFORE INSERT
  ON `ena`
  FOR EACH ROW
BEGIN 


IF NEW.TAG_CODICE = '' THEN
  CALL P_CAMPO_VUOTO('ena', 'tag_codice');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
