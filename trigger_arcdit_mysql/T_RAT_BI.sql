CREATE TRIGGER T_RAT_BI_0
  BEFORE INSERT
  ON `rat`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0  THEN
  CALL P_CAMPO_VUOTO('rat','progressivo');
END IF;

IF NEW.CAMBIO = 0 THEN
  CALL P_CAMPO_VUOTO('rat', 'cambio');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
