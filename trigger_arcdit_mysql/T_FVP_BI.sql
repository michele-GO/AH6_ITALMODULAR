CREATE TRIGGER T_FVP_BI_0
  BEFORE INSERT
  ON `fvp`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('fvp', 'progressivo');
END IF;

IF NEW.RATA = 0 THEN
  CALL P_CAMPO_VUOTO('fvp', 'rata');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
