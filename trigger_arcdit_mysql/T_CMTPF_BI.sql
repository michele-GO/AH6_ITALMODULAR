CREATE TRIGGER T_CMTPF_BI_0
  BEFORE INSERT
  ON `cmtpf`
  FOR EACH ROW
BEGIN 


IF NEW.ID_CMT = 0 THEN
  CALL P_CAMPO_VUOTO('cmtpf', 'id_cmt');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
