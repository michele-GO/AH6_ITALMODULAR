CREATE TRIGGER T_DAT_BI_0
  BEFORE INSERT
  ON `dat`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('dat', 'progressivo');
END IF;

IF NEW.CAMBIO = 0 THEN
  CALL P_CAMPO_VUOTO('dat', 'cambio');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
