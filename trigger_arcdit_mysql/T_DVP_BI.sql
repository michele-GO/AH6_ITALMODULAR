CREATE TRIGGER T_DVP_BI_0
  BEFORE INSERT
  ON `dvp`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('dvp', 'progressivo');
END IF;

IF NEW.RATA = 0 THEN
  CALL P_CAMPO_VUOTO('dvp', 'rata');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
