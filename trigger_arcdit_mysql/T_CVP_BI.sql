CREATE TRIGGER T_CVP_BI_0
  BEFORE INSERT
  ON `cvp`
  FOR EACH ROW
BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('cvp', 'progressivo');
END IF;

IF NEW.RATA = 0 THEN
  CALL P_CAMPO_VUOTO('cvp', 'rata');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
