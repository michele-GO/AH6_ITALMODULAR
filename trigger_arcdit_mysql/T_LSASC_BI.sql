CREATE TRIGGER T_LSASC_BI_0
  BEFORE INSERT
  ON `lsasc`
  FOR EACH ROW
BEGIN 


IF NEW.ID_LSA = 0 THEN
  CALL P_CAMPO_VUOTO('lsasc','id_lsa');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
