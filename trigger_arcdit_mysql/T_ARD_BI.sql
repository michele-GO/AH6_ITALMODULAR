CREATE TRIGGER T_ARD_BI_0
  BEFORE INSERT
  ON `ard`
  FOR EACH ROW

BEGIN 


IF NEW.PROGRESSIVO = 0 THEN
  CALL P_CAMPO_VUOTO('ard', 'progressivo');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
