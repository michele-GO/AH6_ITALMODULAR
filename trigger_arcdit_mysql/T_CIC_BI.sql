CREATE TRIGGER T_CIC_BI_0
  BEFORE INSERT
  ON `cic`
  FOR EACH ROW
BEGIN 


IF NEW.ART_CODICE = '' THEN
  CALL P_CAMPO_VUOTO('cic', 'art_codice');
END IF;

IF NEW.SEQUENZA = 0 THEN
  CALL P_CAMPO_VUOTO('cic', 'sequenza');
END IF;

IF NEW.ID IS NOT NULL THEN
  SET NEW.ID = null;
END IF;

END
